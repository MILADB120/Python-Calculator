# 🧮 Python Tkinter Calculator

A clean, functional Graphical User Interface (GUI) calculator built using Python's built-in **Tkinter** library. This project features a responsive grid layout, arithmetic operations, and basic error handling.

## 🚀 Features
* **Standard Operations:** Supports Addition, Subtraction, Multiplication, and Division.
* **Error Handling:** Prevents crashes by catching `ZeroDivisionError` (dividing by zero) and `SyntaxError` (invalid inputs).
* **User Interface:** * Responsive number pad (1-9) generated via loops.
    * Color-coded operator buttons for better visibility.
    * "AC" (All Clear) button to reset the display.
* **Fixed Geometry:** The window is set to `resizable(False, False)` to maintain the UI layout integrity.

## 🛠️ Requirements
* **Python 3.x**
* **Tkinter** (usually included with standard Python installations).

## 💻 How to Run
1. Ensure you have Python installed on your system.
2. Save the code as `Python-Calculator.py`.
3. Open your terminal or command prompt.
4. Run the following command:
   ```bash
   python Python-Calculator.py
